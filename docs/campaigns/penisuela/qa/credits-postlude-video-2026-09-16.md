# Послетитровый ролик — 16 сентября 2026

## Полученный файл

По сообщению автора «ролик в загрузках» подключён `assets/concepts/campaigns/penisuela/scene-videos/post-credits-dragon.mp4`, без перекодирования, обрезки и ускорения. Совпадение с предоставленным MP4 проверено побайтно. SHA-256: `95ef725daf1b256a3ff4a4358f0cde9b87bab11bc3b0525691325e6cc900a769`.

Фактические параметры: 10,041667 с, 1264×720, H.264, 24 fps, AAC stereo 48000 Гц; 6 550 962 байта. Плановый промпт был рассчитан на 8 с; авторский результат воспроизводится целиком до нативного события `ended`.

Просмотрены извлечённые кадры на 0, 4, 7,5 и 9,7 с: исходный дракон лежит неподвижно, рука в чёрной перчатке входит справа и остаётся на лбу, лицо посетителя не видно. Технически проверено наличие звуковой дорожки с ненулевым сигналом: средний уровень −16,2 dB, пик −2,8 dB. Раздельная проверка речи и музыки на слух не выполнялась; исходная дорожка сохранена полностью.

## Поведение и изменения

- `content/campaigns/penisuela-credits.json` получил документированное поле `postCreditsVideo`; файл зарегистрирован в manifest. Гайд, video-plan, production pack и workflow синхронизированы.
- Общий тизер следует за лентой обеих концовок. Он не меняет сохранение, флаги, награды или результат кампании. Состав группы остаётся прежним: пять героев.
- По окончании прокрутки песня прекращается, идёт чёрная пауза 500 мс, затем видео со встроенным звуком, `loop=false`, `playbackRate=1`, громкость 100%. Завершение или ошибка видео открывают финальную благодарность.
- Слой features сохраняет один `HTMLVideoElement`, способный воспроизвести и MP3, и MP4. Действие завершения истории запускает его синхронно в пользовательском жесте; загрузка маршрута и React StrictMode не заменяют плеер. После песни меняется источник того же элемента. Повторный вход возвращает MP3 и начальную громкость 50%.
- `CreditsPostlude` использует этот элемент на чёрном фоне с `object-fit: contain`. Экранные и нативные кнопки отключены. Таймер, медиасобытия, восстановление автозапуска и очистка находятся в модели feature.

## Проверки

- `npm run typecheck` — пройдено.
- `npm run build` — пройдено.
- `npm run build:pages` — пройдено; итоговый MP4 в dist совпадает с каноническим файлом побайтно.
- `test-credits-music.ts` через штатный TypeScript runner — пройдено: повторные отказы autoplay, AbortError, отсутствие перекрывающихся play, остановка и игнорирование поздних результатов.
- `test-credits-music-handoff.ts` — пройдено: синхронный старт, один элемент при навигации и StrictMode, остановка песни на чёрном интервале, однократное видео со звуком и исходной скоростью, завершение только по `ended`, удаление слушателей, отмена до запуска, восстановление MP3 при повторном входе.
- `test-credits-roll.ts` — пройдено: 20 фото, 15 групп, 23 имени; разметка без кнопок и дублирующего аудиоплеера.
- `test-campaign-credits.ts` — пройдено: обе концовки, персонажи, все фото, музыка и послетитровый ролик существуют в каноническом manifest; пауза и громкость валидны.
- `jq empty`, уникальность ID, ссылки на обе концовки, сверка manifest/JSON/workflow/гайда, SHA-256 исходника и `git diff --check` — пройдено.

Браузерная проверка не выполнялась по повторному указанию автора «Заверши без браузера». Фактическое воспроизведение в браузере и визуальная адаптивность не заявляются проверенными. Сборки сохраняют прежнее предупреждение о размере JS-чанков. Commit, push и deployment не выполнялись; это целевая проверка титров, а не приёмка всей кампании.
