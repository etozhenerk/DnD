# План реализации интерактивной Пенисуэлы

Статус: утверждён пользователем командой «запускай сабагента на реализацию».

Актуальный релизный статус: Delta 1–9 реализованы. Исторические ограничения ранних delta описывают границы на момент их выпуска; текущий исполняемый scope продолжается после `guest-bungalows` через поздний сюжет, обязательный финал и три эпилога.

## Цель первого среза — историческая граница Delta 1

Первый срез собирал публичное превью пролога «Пять браслетов без имён»: девять утверждённых слайдов от таверненной реплики Лорда Крайнеплота до полёта Кострюльки с приглашением. Это была самостоятельная историческая граница до полного интерактивного прохождения; бои, инвентарь, состояния, диалоговые развилки и панель мастера не входили именно в Delta 1, но реализованы последующими delta.

## Изменения схемы и порядок миграции

1. Описать в `content/README.md` публичную сущность `campaignPreview`.
2. Добавить `content/campaigns/penisuela-preview.json` со следующей схемой:
   - `version`, `id`, `campaignId`, `regionId`, `title`, `eyebrow`, `status`;
   - `slides[]`: `id`, `order`, `image`, `alt`, `speaker`, `text`;
   - `speaker`: `kind` (`narrator` или `character`), `label`, необязательный `characterId`.
3. Добавить синхронный `content/campaigns/penisuela-preview-guide.md` с тем же порядком кадров и реплик для мастера.
4. В Delta 1 не менять `world-map.json.campaignId`: на тот момент существовало только превью. В актуальном релизе поле связано с `penisuela`, а регион имеет статус `ready`.

Новых механик и полей основной сущности `Campaign` в этом срезе нет. Девять кадров и универсальная книжная подложка опубликованы в `assets/concepts/manifest.json`; дополнительных зависимостей не требуется.

## Канонический публичный текст

Порядок берётся из утверждённого `art-plan.json`:

1. Лорд Крайнеплот: «Да у меня там свои люди. Решаемо.»
2. Рассказчик: неизвестный посетитель замечает реплику Лорда.
3. Рассказчик: незнакомец направляется к его столу.
4. Рассказчик: он молча передаёт чёрный конверт.
5. Рассказчик: Крайнеплот замолкает и рассматривает печать.
6. Рассказчик: предметный план запечатанного конверта.
7. Рассказчик: внутри видны пять золотых пропусков и край письма.
8. Лорд Крайнеплот: «Найдёшь пятерых. Если спросят, кто женится, — значит, приглашение составлено правильно.»

Точный текст JSON и гайда должен совпадать с полем `audioCue.line` утверждённой раскадровки без добавления спойлеров о женихе, Алексис или событиях после пробуждения.

## FSD и переиспользование

### Entities

- `src/entities/campaign-preview/model/types.ts` — типы превью и слайда.
- `src/entities/campaign-preview/model/data.ts` — единственный импорт канонического JSON.

Существующую сущность завершённой `Campaign` не расширять: у превью другой жизненный цикл и существенно меньшая публичная схема.

### Features

- `src/features/navigate-campaign-preview/model/useCampaignPreview.ts` — текущий индекс, переход вперёд/назад, выбор кадра, Home/End и граничные состояния.

Навигационная логика не хранится в странице или `app`. Автопроигрывание в MVP не добавляется: мастер сам задаёт темп рассказа.

### Widgets

- `src/widgets/campaign-preview/ui/CampaignPreview/CampaignPreview.tsx`;
- `src/widgets/campaign-preview/ui/CampaignPreview/CampaignPreview.module.css`.

Виджет показывает один кадр через `resolveAsset`, нижнюю книжную легенду, имя рассказчика, переиспользованные стрелки книги и девять доступных маркеров прогресса. Арт вписывается целиком примерно в 70% окна через `object-fit: contain`; окружение заполняет затемнённая размытая копия текущего кадра.

### Pages и app

- `src/pages/campaign-preview/ui/CampaignPreviewPage/CampaignPreviewPage.tsx` — берёт готовые данные и компонует страницу без бизнес-логики.
- `src/pages/campaign-preview/ui/CampaignPreviewPage/CampaignPreviewPage.module.css` — полноэкранная сцена без прокрутки.
- `src/app/App.tsx` — отдельный lazy-маршрут `/campaign/penisuela/prologue` вне обычного `AppLayout`, чтобы верхнее меню не уменьшало кинематографичный кадр.
- `src/widgets/planned-campaign/ui/PlannedCampaign/PlannedCampaign.tsx` — для региона `penisuela` появляется кнопка «Смотреть пролог»; остальные будущие регионы не меняются.

На экране пролога есть собственная доступная ссылка «Вернуться к Пенисуэле». В Delta 1 кнопка после девятого кадра возвращала на `/region/penisuela`; последующие delta заменили эту временную границу прямым переходом в опубликованное пробуждение.

## Визуальное поведение

- Экран занимает ровно `100dvh × 100vw` и не создаёт скролл.
- Кадр сохраняет исходное соотношение сторон и не обрезает лица, руки, конверт или Кострюльку.
- Подпись остаётся читаемой с общего экрана на ширинах 1024 и 1280 px.
- Стрелки доступны мышью и клавиатурой; поддерживаются `ArrowLeft`, `ArrowRight`, `Home`, `End`.
- Текущий слайд объявляется через `aria-live="polite"`; каждому изображению задан содержательный `alt`.
- `focus-visible` заметен, а при `prefers-reduced-motion` смена кадров происходит без анимации.
- Все новые цвета, альфа-значения, градиенты и тени сначала добавляются переменными в `src/shared/styles/variables.css`; прямых цветов в CSS Modules нет.

## Следующие этапы и зависимости

1. **Публикация данных превью** — схема, JSON и синхронный гайд. Зависит только от утверждения этого плана.
2. **Модель и навигация** — типы, импорт данных, чистая индексная логика. Зависит от шага 1.
3. **Полноэкранный виджет** — слайд, подпись, кнопки и прогресс. Зависит от шага 2.
4. **Маршрут и вход с региона** — lazy route, страница и кнопка «Смотреть пролог». Зависит от шага 3.
5. **Проверка первого среза** — typecheck, build, build:pages и встроенный браузер. Зависит от шага 4.
6. **Следующий отдельный срез после новой приёмки** — пробуждение в люксе, письмо, браслеты на героях и первые действия. Он не является частью текущего плана кодирования.

Для Delta 1 rules engine, NPC behavior, диалоговая консоль, GM console и бой были отдельными зависимыми этапами и не влияли на модель пролога. Все они входят в актуальный реализованный release scope.

## Затрагиваемые файлы первого среза

- `content/README.md`
- `content/campaigns/penisuela-preview.json`
- `content/campaigns/penisuela-preview-guide.md`
- `src/entities/campaign-preview/model/types.ts`
- `src/entities/campaign-preview/model/data.ts`
- `src/features/navigate-campaign-preview/model/useCampaignPreview.ts`
- `src/widgets/campaign-preview/ui/CampaignPreview/CampaignPreview.tsx`
- `src/widgets/campaign-preview/ui/CampaignPreview/CampaignPreview.module.css`
- `src/pages/campaign-preview/ui/CampaignPreviewPage/CampaignPreviewPage.tsx`
- `src/pages/campaign-preview/ui/CampaignPreviewPage/CampaignPreviewPage.module.css`
- `src/widgets/planned-campaign/ui/PlannedCampaign/PlannedCampaign.tsx`
- `src/widgets/planned-campaign/ui/PlannedCampaign/PlannedCampaign.module.css`
- `src/shared/styles/variables.css`
- `src/app/App.tsx`

## Критерии готовности

- JSON проходит `jq empty`, все девять `id` уникальны, `order` непрерывен от 1 до 9.
- Пути каждого слайда существуют и зарегистрированы в `assets/concepts/manifest.json`.
- JSON и гайд содержат одинаковый порядок, говорящих и текст.
- `/campaign/penisuela/prologue` открывается напрямую и после перехода со страницы региона.
- Мышь, `ArrowLeft`, `ArrowRight`, `Home`, `End` и маркеры прогресса приводят к ожидаемому кадру и не выходят за границы 1–9.
- На первом кадре кнопка назад недоступна; на последнем появляется явное завершение превью.
- Нет ошибок в консоли, горизонтального или вертикального переполнения.
- Проверены viewport 1280 × 720, 1024 × 768 и мобильная компоновка 390 × 844 во встроенном браузере Codex.
- Выполнены `npm run typecheck`, `npm run build` и `npm run build:pages`.

## Риски и защита от них

- **Смешение превью и полной кампании:** отдельная сущность и маршрут не переводят регион в статус готовой кампании.
- **Спойлеры:** в JSON попадает только текст девяти утверждённых кадров; закрытые развилки и содержание путешествия не публикуются.
- **Кадры разного фактического размера:** контейнер использует единое соотношение сторон и `object-fit: contain`, поэтому слайд 4 не прыгает и не обрезается.
- **GitHub Pages:** маршрут проверяется через `build:pages` и существующий SPA fallback; все изображения разрешаются через `resolveAsset`.
- **Разрастание интерфейса до готовности механик:** первый срез не импортирует боевые рамки, жетоны, состояния или GM-панель.

---

# Delta 2: пробуждение в `hotel-overload`

Статус: утверждён и уточнён после первой визуальной приёмки. Первый срез пролога остаётся реализованным и не переделывается, кроме конечной ссылки.

## Цель второго среза

После девятого слайда пролога открыть первый настоящий игровой экран: пятеро героев просыпаются в разгромленном номере Пенисуэлы, мастер читает вступление и вместе с игроками последовательно открывает найденные объекты.

Срез должен ощущаться как начало интерактивного квеста, но не изображать готовый rules engine. В него входят:

- полноэкранная сцена разгромленного номера;
- художественный текст мастера без технических пояснений в игроковой части;
- пять утверждённых жетонов героев как постоянный состав партии;
- отдельная сцена осмотра комнаты с обратного ракурса;
- самостоятельный поиск переносного пульта, письма, браслетов и повреждённой записи;
- инвентарь, из которого открываются найденные сведения;
- подготовленный переход в гостиничную галерею;
- локальное восстановление текущего шага после обновления страницы.

Не входят: броски d20, автоматическое применение DC, бой, инициатива, HP/AC, NPC behavior, полноценная диалоговая консоль и общий GM console. Они подключаются следующими специализированными этапами поверх той же модели сцены.

## Переход после пролога

- На последнем слайде заменить «Завершить пролог» на художественную кнопку «Проснуться на Пенисуэле».
- Кнопка ведёт на `/campaign/penisuela/play/hotel-overload`.
- Видео пока не имеет маршрута и заглушки: переход идёт напрямую в комнату.
- Позже видео вставляется как промежуточный route handoff между прологом и `hotel-overload`, не меняя URL самой игровой сцены.

## Изменения схемы и публикация данных

До React-кода применить `publish-dnd-campaign` к минимальному публичному срезу сцены. Не импортировать данные напрямую из `docs/campaigns/penisuela/script.md` или `gameplay.json`.

Добавить и описать в `content/README.md` сущность `campaignSessionPreview`:

```text
version, id, campaignId, regionId, status, initialSceneId
party[]: characterId, token, label
scenes[]:
  id, title, eyebrow, background, alt, readAloud, roomLegend
  inspectables[]: id, label, image, icon, locationHint, summary, revealText, useText, order
  exit: label, nextSceneId, availableAfter[]
```

Добавить:

- `content/campaigns/penisuela-session-preview.json` — только публичные сведения `hotel-overload`, без скрытой правды мастера и будущих спойлеров;
- `content/campaigns/penisuela-session-preview-guide.md` — синхронный человекочитаемый порядок вступления, осмотров и перехода.

Первый JSON не хранит DC, провалы, флаги кампании или секреты финала. Для второго среза он содержит четыре наблюдаемых объекта:

1. `overload-console` — на экране видны пятичасовой таймер, снятая защита и два пустых голосовых поля;
2. `journey-letter` — напоминает только об оплаченном пути, закрытом празднике и просьбе помочь организаторам;
3. `anonymous-bracelets` — действующие пропуска гостиничного VIP-маршрута;
4. `egorik-recording` — повреждённый стоп-кадр с Егориком, Настасьей, конвертом и служебным входом без объявления Егорика женихом.

Пути изображений и иконок брать только из опубликованных записей `assets/concepts/manifest.json`.

## FSD-слои

### Entities

- `src/entities/campaign-session/model/types.ts` — публичные типы сессии, сцены и осматриваемого объекта;
- `src/entities/campaign-session/model/data.ts` — единственный импорт канонического JSON;
- `src/entities/campaign-session/ui/InspectableArtifact/InspectableArtifact.tsx` и CSS Module — точка поиска, инвентарная иконка и художественное раскрытие найденного предмета;
- `src/entities/campaign-session/ui/HeroToken/HeroToken.tsx` и CSS Module — отображение пяти утверждённых круглых жетонов.

### Features

- `src/features/navigate-campaign-scene/model/useCampaignScene.ts` — шаг вступления, открытые предметы и доступность выхода;
- `src/features/navigate-campaign-scene/model/sessionStorage.ts` — сериализация только `sceneId`, `introRead` и `revealedInspectableIds` в `localStorage` с версией схемы;
- `src/features/navigate-campaign-scene/ui/SceneAdvanceControl/SceneAdvanceControl.tsx` — одна художественная кнопка текущего шага без слов «state», «phase», «debug» и других технических подписей.

На этом этапе feature не бросает кубы и не вычисляет игровые эффекты.

### Widgets

- `src/widgets/campaign-scene/ui/CampaignScene/CampaignScene.tsx` и CSS Module — полноэкранная композиция;
- переиспользовать `quest-shell-frame.png` как обрамление сцены и `universal-panel-frame.png` как рамку раскрытого объекта;
- переиспользовать утверждённые item art и inventory icons;
- не использовать боевые рамки, initiative rail и dialogue frame до соответствующих игровых этапов.

### Pages и app

- `src/pages/campaign-play/ui/CampaignPlayPage/CampaignPlayPage.tsx` — выбирает опубликованную сцену и компонует виджет без бизнес-логики;
- `src/pages/campaign-play/ui/CampaignPlayPage/CampaignPlayPage.module.css` — `100dvh × 100vw`, без прокрутки;
- `src/app/App.tsx` — lazy route `/campaign/penisuela/play/:sceneId` вне обычного `AppLayout`;
- `CampaignPreview.tsx` — последняя кнопка ведёт в `hotel-overload`, а не обратно в регион.

## Композиция экрана

- Утверждённый `hotel-overload.png` занимает основной фон без обрезки ключевых героев и предметов.
- Внизу располагается книжный блок мастера с `readAloud`; размер текста рассчитан на общий экран 1024 и 1280 px.
- На экране пробуждения остаются только сюжетный кадр и текст мастера; жетоны и предметы не перекрывают сцену.
- Действие «Осмотреть номер» переводит на `hotel-overload-search` с обратным ракурсом пустого люкса.
- Нижний блок остаётся единым рассказом мастера: `roomLegend` продолжает вводный текст одним абзацем и естественно связывает места находок, заметные детали и направление дальнейшего поиска без перечня игровых функций.
- Предметы находятся через точки интереса, сразу открывают полное описание и одновременно появляются без подписей в точных ячейках инвентаря; заметный hover и `focus-visible` показывают интерактивность.
- Красная отметка на сумке показывает только найденные, но ещё не просмотренные описания.
- Игроковая часть не показывает DC, идентификаторы, флаги и инструкции разработчика.
- Для мастера на этом срезе нет отдельного drawer: он управляет раскрытием с общего экрана кликами и клавиатурой.
- Мобильная версия не реализуется; целевые viewport — 1280 × 720 и 1024 × 768.

## Порядок реализации

1. Утвердить этот delta-план.
2. Опубликовать минимальные данные `hotel-overload` и синхронный гайд через `publish-dnd-campaign`.
3. Добавить entity-модель и проверить пути всех артов и иконок через manifest.
4. Добавить feature состояния сцены и восстановление из `localStorage`.
5. Собрать полноэкранный `CampaignScene` и четыре предметных раскрытия.
6. Добавить игровой маршрут и перевести финальную кнопку пролога на него.
7. Запустить `npm run typecheck`, `npm run build`, `npm run build:pages`.
8. Проверить встроенным браузером прямой маршрут, переход из пролога, клавиатуру, восстановление после refresh и viewport 1024/1280.

## Критерии готовности

- После последнего слайда пролога открывается `/campaign/penisuela/play/hotel-overload`.
- На сцене нет технического интерфейсного текста и спойлеров мастера.
- Фон, пять жетонов, четыре предмета и все иконки разрешаются через `resolveAsset` и зарегистрированы в manifest.
- Вступление можно завершить мышью и клавиатурой; отдельная сцена осмотра открывается без промежуточной панели предметов.
- Точки интереса открывают текст раскрытия и добавляют предметы в инвентарь; повторное чтение доступно из инвентаря.
- После обновления страницы сохраняются прочитанное вступление и уже раскрытые предметы.
- Повреждённая запись не объявляет Егорика женихом.
- Головач остаётся без хвоста; у Линды не пропадает половина крыльев в используемых артах.
- Нет вертикального или горизонтального переполнения на 1280 × 720 и 1024 × 768.
- `typecheck`, `build` и `build:pages` проходят без ошибок.

## Delta 3 — игровая гостиничная галерея

Статус: реализован по прямому запросу пользователя вернуть вариативность утверждённого сюжетного графа.

- `content/campaigns/penisuela-gallery-gameplay.json` отделяет исполняемые условия дверей, диалоги, проверки и встречу от презентационных кадров.
- `GallerySessionSnapshot` строится повторным проигрыванием версионированного журнала событий; броски записываются готовыми значениями, поэтому перезагрузка воспроизводит тот же результат.
- В холле видны три направления. Левая золотая дверь и подъём по лестнице доступны сразу; центральная дверь открывается только ключом Pussy Sultan. До поручения на верхней галерее доступен только осмотр паланкина, а взаимодействие со скипетром скрыто.
- Интерактивные области не имеют видимых технических подписей и при hover или `focus-visible` лишь мягко подсвечивают архитектурный объект. Художественное описание общего экрана выводится только через общий `SceneTextPanel`.
- Аудиенция Pussy Sultan начинается с одной проверки доверия Харизмой: Бубсильда и Линда проходят её против DC 10, остальные герои — против DC 15. Успех открывает поручение со скипетром и единственный маршрут к Вуманайзеру. После провала остаются уход или одна проверка запугивания Харизмой DC 17; её успех выдаёт ровно три пропуска в бар, а провал немедленно начинает ранний бой с тремя VIP-стражниками. Победа также выдаёт три пропуска и никогда не открывает Вуманайзер.
- Все действия этой ветки передаются в общий `SceneMasterControl` как `SceneMasterAction`. При нажатии на проверяемое решение меню мастера закрывается, а внебоевые проверки доверия, запугивания и трёх способов поднять паланкин открываются в общем `SceneCheckPanel`, переиспользующем `SceneDecisionModal`. Кнопки не раскрывают характеристику, DC или последствие провала; эти данные видны только в модальном окне. Цифровой бросок проходит через общий `D20Roller`, физические 1/20 — через единый critical-roll effect; adventure-компонент не содержит отдельной формы броска или описательных карточек проверки в `masterContent`.
- До получения доступа ветка не предлагает ложных отказов и выходов: после провала доверия обязательным продолжением становится запугивание, после его провала автоматически начинается бой; принятое поручение ведёт к одному из трёх способов освободить скипетр, а возвращение скипетра — к обязательному получению Вуманайзера и прохода в бар. Навигация назад остаётся служебной отменой мастера ровно одного действия, а не сюжетным отказом.
- Текст общей легенды аудиенции отражает сохранённый исход: начальную реплику, провал доверия, успех доверия, принятое поручение, успешное запугивание либо победу над стражниками.
- В реквизиторской нет дублирующих интерактивных областей поверх арта: выбор способа, подбор освобождённого скипетра и возврат к Pussy Sultan выполняются только кнопками общей панели мастера.
- Алексиса можно убедить обычной проверкой, автоматическим умением Торина или преимуществом Ламберта. После отказа обязательный маршрут остаётся доступен через поиск в счетах.
- Кракен допускает автоматический обход Линдой, общую проверку механизма или полноценный бой с инициативой, HP, AC, атаками героев, слабостью, решением NPC и override мастера.
- Инвентарь остаётся общим между сценами; награды ветки добавляются и удаляются вместе с игровыми событиями, а сюжетный предмет Pussy Sultan сохраняет позднее назначение.
- Поддержаны физический d20, цифровой бросок, отмена ровно одной последней команды, полный сброс и восстановление после refresh.

Презентационные сцены галереи сохраняют арты и тексты, но имеют `exit: null`: прямые URL также открывают игровой контроллер, поэтому линейную цепочку кнопок нельзя обойти адресом.

## Delta 4 — поручение Прохора в закрытом баре

Статус: реализован и принят после утверждения пользователем командой «го». Контент, журнал событий, fail-forward и маршрутизация подключены; терминальные и браузерные проверки пройдены на 1024 и 1280 px.

### Объём

- Сохранить один маршрут `closed-bar-prokhor`, но переключать его публичное состояние по журналу событий.
- Опубликовать три презентационных варианта через существующую схему `CampaignSessionScene`: плательщица установлена, терминал выдал график и бейдж, оплата подтверждена звонком.
- Подключить предметные модалки утверждённых счёта, активного терминала и терминала с графиком.
- Оставить прямой вопрос, запуск аудита, скрытые проверки и подтверждающий звонок только в меню мастера.
- После звонка открыть явный переход к Алексис; действие «Рассказать про Успенскую» показывает карточку ключа от гримёрки на повторно используемом утверждённом арте и после закрытия добавляет самостоятельный предмет `alexis-dressing-room-key` в инвентарь.

### Изменения схемы

Новых полей нет. Варианты используют существующие `scenes[]` и `inspectables[]`; значение `backgroundLayout: contain` сохраняет утверждённые кадры Прохора целиком без увеличения и обрезания. Формальные исходы остаются в `penisuela-gallery-gameplay.json`, а состояние хранится флагами версионированного журнала.

### Состояния и события

1. `prokhor-conversation-started` открывает подсветку счёта.
2. `prokhor-bill-inspected` показывает предметную модалку счёта и подсвечивает выключенный терминал.
3. `prokhor-terminal-inspected` открывает крупный план терминала без предупреждения и действие запуска аудита.
4. `prokhor-payment-audit-started` показывает активный терминал и скрытые проверки через общий физический/цифровой d20:
   - Ламберт, Интеллект DC 12;
   - Бубсильда, Харизма DC 12.
5. Успех одной проверки или провал обеих устанавливает `prokhor-payer-identified`.
6. Провал обеих дополнительно устанавливает `prokhor-work-schedule-issued`, переключает большой арт на график и открывает его предметный крупный план.
7. Отдельное действие мастера «Подтвердить оплату звонком» устанавливает `prokhor-payment-confirmed`, `prokhor-funding-revealed` и `alexis-prokhor-task-completed`, после чего большой арт переключается на телефонный звонок.

Версия журнала повышается, потому что прежний исход проверки сразу завершал поручение и несовместим с новой двухшаговой подачей.

### FSD

- `content/campaigns/penisuela-session-preview.json` и гайд: публичные варианты сцены и предметные тексты.
- `features/navigate-campaign-scene/model/useGallerySession.ts`: события проверок, fail-forward, звонок и undo.
- `widgets/campaign-scene/ui/ClosedBarAdventure`: выбор канонического состояния, модалки предметов и действия мастера.
- Существующие `CampaignScene`, `SceneMasterControl`, `SceneDecisionModal` и `InspectableArtifactDialog` переиспользуются без нового слоя и зависимостей.

### Критерии готовности

- Секретные варианты проверок не видны до раскрытия в модалке мастера.
- После первого провала остаётся второй подход; после второго провала появляется график и путь не блокируется.
- Сцена звонка появляется только после отдельного действия мастера.
- Во всех состояниях сохраняются счёт, терминал, телефон, график/бейдж и коктейль согласно утверждённым артам.
- Переход к Алексис появляется только после звонка.
- Шаг назад откатывает проверку, график и звонок через общий журнал.
- После refresh воспроизводится тот же этап.
- `jq empty`, `npm run typecheck`, `npm run build` и `npm run build:pages` проходят; маршрут проверен во встроенном браузере на 1024 и 1280 px без переполнения и ошибок консоли.

При приёмке дополнительно исправлены перекрытие интерактивных областей счёта и терминала нижней легендой, неработающий полный сброс интерактивного этапа и конфликт React-ключей одноимённых NPC из разных кампаний. Детерминированно пройдены успешный исход, последовательность из двух провалов с выдачей графика, undo звонка и проверки, награда Алексис и восстановление состояния после refresh.

## Delta 5 — живой разговор со Стасом и пропуск Ламберту

Статус: исправлен после прямого уточнения пользователя. Предыдущая версия с экранными репликами и без пропуска признана неверной.

### Объём

- Сохранить подвид `closed-bar?view=stas` и утверждённый крупный план Стаса у стойки; переход открывается кликом по его силуэту в общем кадре.
- Публичная литературная панель содержит только описание видимой сцены; реплики, сведения и результаты разговора мастер произносит вживую.
- Не показывать игрокам кнопок в подвиде Стаса. Дать мастеру одно действие «Выдать пропуск Ламберту» без броска и без дополнительных развилок.
- Одной командой установить `stas-met`, `stas-information-received`, `stas-recognized-lambert`, `stas-bungalow-pass-issued`, `stas-bungalow-pass-held-by-lambert`, раскрыть `egorik-lead` и добавить один `stas-bungalow-pass` в инвентарь Ламберта.
- Стас узнаёт браслет Егорика, знает, что их бунгало стоят рядом, узнаёт в Ламберте друга и поэтому отдаёт пропуск именно ему.
- Не выдавать у Стаса `dressing-room-route` или иной способ обойти танцоров.

### Данные, FSD и миграция

- `content/campaigns/penisuela-gallery-gameplay.json` хранит только мастерские ориентиры разговора; готовых реплик сцены в банке диалогов нет.
- `features/navigate-campaign-scene/model/useGallerySession.ts` записывает выдачу пропуска, владельца предмета и связанные флаги в общий воспроизводимый журнал.
- `widgets/campaign-scene/ui/ClosedBarAdventure` всегда показывает одно и то же описание рассказчика; сюжетная кнопка находится только в панели мастера и после выдачи исчезает.
- Версия локальной сессии повышается, потому что черновые события прежней механики Стаса несовместимы с исправленным сюжетом.

### Критерии готовности

- До и после действия мастера игроки видят только описание сцены со Стасом, без его реплик и без публичных кнопок.
- Одно действие выдаёт ровно один пропуск Ламберту; бросок, выбор героя и характеристики отсутствуют.
- Полученная информация не открывает гримёрку и не завершает основной путь бара.
- Undo, полный сброс и refresh воспроизводят передачу пропуска через общий журнал.
- `jq empty`, `npm run typecheck`, `npm run build` и `npm run build:pages` проходят; маршрут проверен во встроенном браузере на 1024 и 1280 px без переполнения и ошибок консоли.

## Delta 6 — танцоры и путь через гримёрку

Статус: реализован и целево проверен после утверждения пользователем повторяемого наказания за неверный трек.

- На общем плане бара центральный проход ведёт к зацикленной танцевальной труппе; Стас остаётся отдельным боковым разговором. Прохор из сцены удалён.
- Труппа изначально танцует под заколдованную музыку: на финальной vogue-позе трек сбрасывается и принудительно запускает номер заново.
- Освободить танцоров можно только музыкальной головоломкой без d20: заметить четыре доли подсказки и выбрать дорожку с чётким клубным four-count.
- Два неверных трека после отдельного подтверждения показывают обязательную модалку «трек не подходит, пульт призвал охрану», помечаются как уже проверенные и создают новую волну из четырёх Бит-стражей.
- Правильный трек устанавливает `dance-correct-track-selected`, `dance-troupe-freed`, `dance-troupe-allies` и раскрывает `dressing-room-route`: танцоры объясняют путь через артистическую гримёрку в закрытую область для пар.
- Танцоры никогда не становятся целями. Бой направлен только против голографических стражей пульта и не добавляет Doom, давление времени или отдельную сюжетную цену.
- Контракт сохраняет заменяемые слоты видео танцевальной петли и аудио заколдованной/выбранной музыки, но UI не выводит технические заглушки без утверждённого `source`. После публикации файла слот появляется без изменения игровой логики.
- Переход в `artists-dressing-room` остаётся следующим срезом; Стас не дублирует эту улику.

### Критерии готовности

- На общем плане доступны Стас и центральный проход к танцорам; Прохора в сцене нет.
- Неверный трек не освобождает труппу, остаётся в журнале, открывает устойчивую к refresh обязательную модалку и откатывается через undo.
- Подтверждение модалки запускает ровно четыре свежие цели; после завершения боя интерфейс возвращается к пульту, а второй неверный трек способен вызвать отдельную новую четвёрку.
- Правильный трек завершает номер и показывает маршрут через гримёрку.
- Refresh воспроизводит последний выбранный трек и состояние освобождения.
- В UI нет технических подписей или пустых медиаблоков; при появлении канонического `source` подготовленный слот становится доступным автоматически.
- `jq empty`, `npm run typecheck`, `npm run build` и `npm run build:pages` проходят; маршрут проверен во встроенном браузере на 1024 и 1280 px без переполнения и ошибок консоли.

При браузерной приёмке пройдены неверная дорожка, undo, правильный four-count, восстановление после refresh и переход по центральной интерактивной области общего плана. На ширине 1024 px дополнительно исправлено унаследованное центрирование нижней легенды: после правки её границы полностью находятся внутри viewport.

## Delta 7 — экран электронного музыкального пульта

Статус: реализован и целево проверен 4 сентября 2026 года.

- Близкий план труппы использует утверждённый арт `assets/concepts/campaigns/penisuela/scenes/closed-bar-dancers.png`; электронный пульт справа открывает отдельный подвид `closed-bar?view=device`.
- Экран пульта строится интерфейсом, а не запечённым текстом в арте, и повторяет форму устройства: три вертикальных неоновых дисплея-дорожки, отдельный видеомонитор, крупный круглый селектор, ромбовидная кнопка запуска, пэды и ползунки.
- Если у канонического видеослота есть `source`, монитор воспроизводит его с управлением, беззвучным автозапуском и циклом. Пока файла нет, тот же монитор показывает утверждённый кадр сцены как действующий сигнал камеры, без технической заглушки.
- Клик по дорожке только выбирает и, при наличии `source`, прослушивает кандидат. Отдельная кнопка «Принять» вызывает журналируемую команду `selectDanceTrack`; до подтверждения флаги и развязка не меняются.
- Неверная дорожка получает состояние «не подходит» и блокируется. Общий `SceneDecisionModal` без крестика и закрытия по фону сообщает об ошибке и призыве охраны; единственное действие запускает `club-beat-guards`.
- Боевая сцена использует утверждённый фон `closed-bar-beat-guards-combat.png`, четыре отдельные цели и общий токен `club-beat-guard.png`. Победа и fail-forward возвращают к устройству; правильный four-count освобождает труппу без боя.
- На самом пульте остаются только органы управления и короткое состояние петли. Литературное описание и реакция на попытку выводятся только через общий `SceneTextPanel` в составе «Рассказчик + описание».
- Все кнопки доступны с клавиатуры, имеют `focus-visible`, а интерфейс помещается на общем экране без внутреннего вертикального скролла.

## Delta 7 — расследование в артистической гримёрке

Статус: утверждён автономным поручением пользователя «делай все без моего ока».

### Объём

- После освобождения танцоров открыть маршрут `artists-dressing-room` и использовать опубликованный общий план гримёрки 1536 × 864 WebP.
- Дать игрокам осмотреть три детали выступления, журнал передачи конверта, зеркало маршрутов и служебное окошко Satyr прямо на сцене.
- После двух деталей выступления открыть проверку Интеллекта или Харизмы DC 12. Успех раскрывает `celebrity-kreed`; провал сохраняет обязательную улику через восстановившийся микрофон и добавляет `time-pressure +1`.
- После журнала и зеркала открыть проверку Мудрости DC 12. И успех, и провал оставляют маршрут к бунгало; провал добавляет `time-pressure +1`.
- Освободить Satyr Ловкостью DC 12 через окошко либо завершить репетицию Харизмой DC 12. Освобождённый Satyr превращает завершение репетиции в автоматический успех.
- После мирного отключения дублёров открыть `stage-module-shutdown`: две повторяемые проверки Интеллекта и Силы DC 12, а `magic-whisper` Линды или `helping-stick` Торина заменяют одну ещё не закрытую линию. Первая реакция системы поглощается подсказкой Satyr, дальнейшие провалы добавляют `time-pressure +1`.
- После отключения установить `stage-power-key`, снять `stage-module-active` и показать границу следующего среза `guest-bungalows` без несуществующего маршрута.
- Историческая граница Delta 7: `dressing-room-double-fight` тогда не публиковался. В финальном релизе ветка реализована на переиспользованном кадре гримёрки и code-native боевом UI; отдельной персональной помехи копирования нет, а каждый герой один раз за бой открывает AC 10 для следующей атаки.

### Изменения схемы

- В `penisuela-gallery-gameplay.json` добавить объект `dressingRoom`: публичные тексты осматриваемых объектов, раскрытий, репетиции и технической ниши.
- Расширить `HeroStat` значением `wisdom` и добавить шесть определений проверок гримёрки/сценического модуля.
- Сессионный журнал переиспользует существующие события флагов, улик, счётчиков, бросков и использований; его версия повышается из-за новых исполняемых команд.

### FSD и маршрутизация

- `entities/campaign-session/model`: типы данных гримёрки и поддержка Мудрости.
- `features/navigate-campaign-scene/model/useGallerySession.ts`: идемпотентные команды осмотра, fail-forward, освобождения Satyr, репетиции и отключения питания.
- `widgets/campaign-scene/ui/ArtistsDressingRoomAdventure`: полноэкранный экран, доступные hotspots, общий d20, скрытые действия мастера и финальная техническая сцена.
- `pages/campaign-play`: только выбор виджета для двух новых scene id; бизнес-логика в page не добавляется.

### Критерии готовности

- Переход в гримёрку появляется только после правильной музыкальной дорожки.
- До двух деталей имя артиста не выводится; ни один публичный текст не называет Kreed женихом.
- Обязательные `celebrity-kreed` и `egorik-lead` сохраняются при провалах через fail-forward.
- Satyr делает репетицию автоматической только после собственного освобождения.
- Технический модуль допускает повтор, замену одной линии и корректно учитывает первую поглощённую реакцию.
- Undo, reset и refresh воспроизводят каждый шаг через общий журнал.
- `jq empty`, `typecheck`, `build`, `build:pages` и браузерная проверка на 1024/1280 и мобильной ширине проходят без переполнения и ошибок консоли.

## Delta 8 — выбор в гостевых бунгало

Статус: реализован и проверен после автономного поручения пользователя «ок го дальше сам все делай».

### Объём

- Открыть публичную сцену `guest-bungalows` на утверждённом фоне `guest-bungalows-choice.webp` после мирного завершения репетиции; из технической ниши вести туда после отключения модуля, а из гримёрки позволить пропустить необязательное отключение.
- Показать Полину, Олву, две половины парного пропуска и пустой стул Стаса без Вуманайзера, руки героя или вида от первого лица в самом арте.
- Дать ровно два взаимоисключающих решения без броска: передать имеющийся Вуманайзер Полине либо позвать Стаса на один добровольный разговор; если предмет найден, во второй ветке он сохраняется.
- Передача Полине требует Вуманайзер в инвентаре. Вызов Стаса требует только ранее установленный `stas-call-consent` и остаётся доступным после враждебной ветки Pussy Sultan — успешного запугивания или победы над тремя стражниками, — где квестовая награда не выдаётся. Недостающее условие блокирует только соответствующий вариант и показывает понятную причину, а не скрытую неработающую кнопку.
- Ветка Полины расходует предмет и устанавливает `womanizer-given-to-polina` и `family-branch-womanizer`. Ветка Стаса сохраняет предмет и устанавливает `family-branch-therapy` и `stas-called-to-session`.
- После выбора показать публичный итог и опубликованный следующий шаг: ветка Вуманайзера предлагает принять просьбу Олвы и перейти в `show-18-pavilion` либо отказаться и сразу открыть `egorik-bungalow-reveal`; ветка Стаса ведёт в `couples-session-stas`.

### Изменения схемы

- В `content/README.md` описать необязательный объект `guestBungalows` внутри существующего `*-gallery-gameplay.json`: вступление, заголовок выбора, две публичные ветки, причины недоступности и тексты разрешения.
- В `penisuela-gallery-gameplay.json` добавить только публичные тексты и стабильные идентификаторы двух веток; скрытая правда и будущие развилки остаются в рабочем сценарии.
- Сессионный журнал переиспользует существующие `flag-changed` и `item-changed`; исходный delta повысил версию до 21, а исправление условия ветки Стаса повышает её до 22, чтобы старое сохранение с обязательным предметом не считалось совместимым.
- Новые зависимости, FSD-слои, типы событий и проверки d20 не требуются.

### FSD и маршрутизация

- `entities/campaign-session/model/galleryGameplay.ts`: тип публичного контракта `guestBungalows`.
- `features/navigate-campaign-scene/model/useGallerySession.ts`: две идемпотентные команды взаимоисключающего выбора; первая проверяет инвентарь, вторая — только согласие Стаса.
- `widgets/campaign-scene/ui/GuestBungalowsAdventure`: полноэкранная сцена, естественно читаемый итог выбора, заблокированные причины, undo и reset.
- `widgets/campaign-scene/ui/ArtistsDressingRoomAdventure`: переход к бунгало после завершения репетиции как с отключением сценического модуля, так и без него.
- `pages/campaign-play`: только выбор нового виджета для `guest-bungalows`; бизнес-логика остаётся ниже страницы.

### Затрагиваемые файлы

- `content/README.md`;
- `content/campaigns/penisuela-session-preview.json` и синхронный `penisuela-session-preview-guide.md`;
- `content/campaigns/penisuela-gallery-gameplay.json`;
- `src/entities/campaign-session/model/galleryGameplay.ts` и `gallerySession.ts`;
- `src/features/navigate-campaign-scene/model/useGallerySession.ts`;
- `src/widgets/campaign-scene/ui/ArtistsDressingRoomAdventure/*`;
- новый `src/widgets/campaign-scene/ui/GuestBungalowsAdventure/*`;
- `src/pages/campaign-play/ui/CampaignPlayPage/CampaignPlayPage.tsx`.

### Критерии готовности

- На фоне нет Вуманайзера и руки героя; Олва показана обычного взрослого роста, а Полина и Олва стоят естественно.
- Каждое условие блокирует только свою ветку с явным объяснением; при согласии Стаса его можно позвать даже без предмета, а при наличии предмета и согласия доступны обе ветки.
- Передача предмета удаляет его из общего инвентаря, вызов Стаса сохраняет; повторный выбор другой ветки невозможен.
- Undo, reset и refresh полностью восстанавливают или отменяют выбор через общий журнал событий.
- Сам экран выбора не раскрывает Егорика, Настасью, Kreed, Angel и свадебные роли; следующий опубликованный маршрут появляется только после фиксации решения.
- `jq empty`, уникальность id, ссылки на manifest, `typecheck`, `build`, `build:pages` и встроенная браузерная проверка на 1280, 1024 и мобильной компоновке проходят без ошибок и горизонтального переполнения.

## Delta 9 — поздний сюжет, обязательный финал и эпилоги

Статус: локальный release scope завершён после автономного поручения пользователя довести кампанию до конца. Production-runtime и integrity-проверки завершены; оставшийся полный production-browser sweep пропущен по явному решению автора и принят как остаточный риск.

### Поздний маршрут

- Опциональный `dressing-room-double-fight` опубликован как три дублёра с разовыми expose-weakness действиями всех пяти героев. Победа освобождает Satyr и ведёт к сценическому модулю; fail-forward возвращает героев с 1 HP и сохраняет прямой маршрут к бунгало без репетиционной команды.
- Ветка Вуманайзера открывает добровольный `show-18-pavilion` либо прямой переход к Егорикам. Ветка Стаса проводит последовательность `couples-session-stas` → `couples-session-polina` → `couples-session-choice`; итог фиксирует только выбранную потребность, а окончательное согласие остаётся за NPC.
- Павильон 18+ закрывается тремя успехами прямого эфира, двумя успехами телесуфлёра либо боем с тремя масками. Два общих провала открывают бой или аварийное завершение. Полная победа выдаёт Красную кнопку с одним зарядом; fail-forward возвращает героям 1 HP и продолжает сюжет без награды.
- Маска Стыда и Маска Универсального рецепта используют спасброски Мудрости DC 12, навешивают краткие состояния и в первом раунде автоматически выбирают разных героев. Олва может один раз снять состояние активного героя.
- В `egorik-bungalow-reveal` проверка голоса и запись Настасьи сохраняют обязательный путь; добровольная помощь паре фиксирует союз на финал. Цифровой контур Егорика и Настасьи даёт один автоматический переброс проваленной финальной проверки канала.
- `groom-tunnel` требует любые три разные линии из четырёх и закрывает повторное использование каждой линии. После двух общих провалов остаётся бой с Кордебалетом конфиденциальности. Победа и ручной fail-forward открывают комнату жениха; аварийный исход возвращает всем 1 HP и добавляет `timePressure +1`.
- Комната жениха проводит освобождение микрофона, стабилизацию двери и подтверждение имени невесты. Необязательный `restore-control-log` требует два разных успеха, даёт преимущество Головачу и Бубсильде на соответствующих линиях и при первом провале сохраняет улику ценой времени.
- `ceremony-villa` фиксирует второй голосовой ключ и отдельное решение о закрытой записи: возврат оригинала, добровольное разрешение технического фрагмента либо использование без согласия. Только зафиксированные флаги открывают соответствующие финальные планы.

### Последний дубль

- `final-choice` выбирает начальную тактику, но не пропускает бой. Encounter `last-take-module` имеет 125 HP и три сегмента с порогами 85, 45 и 0; лишний урон между фазами не переносится.
- В каждой фазе доступны обычная физическая атака и подходящий план: штатный голосовой сброс, режиссёрский монтаж или физическое отключение. Успешный план закрывает ровно текущий сегмент; провал режиссёрского плана немедленно вызывает обратный импульс.
- Репетиционная команда Satyr один раз раскрывает уязвимость после смены фазы, если не была потрачена раньше. Освобождённая танцевальная труппа один раз отменяет ход босса. Красная кнопка отменяет одну ожидающую атаку либо снимает состояние Павильона 18+. Подтверждение ясного выбора расходует заряд и даёт +2 к текущей сюжетной или финальной проверке.
- Победа атомарно выбирает один из трёх опубликованных эпилогов: `wedding-epilogue`, `director-epilogue` или `shutdown-epilogue`. Полное падение партии не убивает героев: аварийный рубильник переводит финал в физическое отключение и возвращает каждого с 1 HP.

### Данные и runtime

- `content/campaigns/penisuela-session-preview.json` содержит 40 публичных презентационных сцен от гостиницы до эпилогов.
- `content/campaigns/penisuela-gallery-gameplay.json` содержит 18 проверок, 6 промежуточных встреч, 16 исполняемых story-scenes, общие счётчики испытаний и декларации fail-forward. Вместе с отдельным encounter `last-take-module` формальный runtime содержит 7 встреч.
- `content/campaigns/penisuela-final-boss.json` является отдельным формальным контрактом трёх фаз, трёх планов и аварийного эпилога.
- `LateStoryAdventure` и `FinalBossAdventure` читают канонические данные, а `useGallerySession` применяет эффекты одним журналом событий. Undo, полный reset и восстановление после refresh работают на всём маршруте.

### Критерии готовности релизного scope

- Из каждой ветки `guest-bungalows` существует исполняемый путь до `last-take-boss` и одного из трёх эпилогов.
- Накопительные испытания не позволяют повторно засчитать неповторяемую дорожку и корректно открывают fail-forward после лимита провалов.
- Каждое полное поражение в обязательном бою сохраняет критический путь без смерти персонажей и применяет ровно объявленные последствия.
- Три фазы босса используют собственные опубликованные фоны, пороги не пропускаются, а выбранный способ последнего сегмента совпадает с эпилогом.
- Общий журнал сохраняет предметы, заряды, HP, условия, отношения, выбранный финал и команды мастера после refresh; undo и полный reset не оставляют состояния сцены или инвентаря.
- Production-runtime suite проходит 10/10 сценариев и 210 assertions; отдельные session/rules, 15/15 NPC и 51/51 final-boss fixtures также проходят.
- `maintain-dnd-world`, `jq empty`, проверка ссылок и уникальности id, `typecheck`, `build` и `build:pages` завершены. Полный финальный production-browser sweep и новые контрольные скриншоты пропущены по решению автора; кампания передана на пользовательское тестирование.

## Delta 10 — первая играбельная сборка актуального сюжета

Статус: реализована как deadline-вертикальный срез 2 сентября 2026 года. Этот раздел заменяет устаревшие runtime-описания Delta 6–9; те разделы сохранены только как история итераций.

### Объём первой сборки

- Девять стартовых экранов и точка будущей видеовставки сохранены; после них игра открывает `hotel-overload`.
- В `hotel-overload` выборы кулона и погодного контура пишутся в общий журнал, после чего маршрут идёт прямо в новый гостиничный хаб без удалённого промежуточного экрана.
- Хаб даёт три независимых кликабельных направления: комнату Алексиса, аудиенцию Pussy Sultan и прямой путь к бару. Поручение Pussy Sultan требует физически получить и вернуть скипетр; Вуманайзер выдаётся только после отдельного принятия награды.
- Все 49 актуальных post-prologue сцен читаются из `penisuela-gallery-gameplay.json` и исполняются одним событийным runtime. Активный путь больше не использует Прохора и Satyr.
- Линия Олвы, Стаса, Полины и Вуманайзера, цепочка Kreed → Grey Wiese → спальня → Angel, добровольные подарки Торину и Бубсильде и четыре финала доступны в коде.
- Сложная трёхфазная машина `Последнего дубля` в этой первой сборке временно сжата до явного выбора уже подготовленного финального решения. Это позволяет провести кампанию целиком; полноценный тактический финал остаётся отдельной следующей дельтой.

### Техническая трасса

- `CampaignPlayPage` выбирает специализированные экраны для старта и гостиничного хаба, затем общий `LateStoryAdventure` для актуального набора `storyScenes`.
- `useGallerySession` остаётся единственным writer флагов, счётчиков, улик и инвентаря; действия хаба также проходят через `commitStoryAction`.
- Рекурсивное `graphCondition` поддерживает `all`, `any`, `not`, flags и сравнения счётчиков; условия синхронизируются из утверждённого story graph.
- `scripts/validate-penisuela-playable-bridge.mjs` проверяет соответствие 49 сцен, ссылки, ключевые цепочки, отсутствие удалённых NPC в активных сценах и достижимость четырёх концовок.

### Критерии готовности

- Реальный browser-smoke проходит от пролога через оба обязательных решения отеля в гостиничную галерею.
- Happy path проходит через Kreed, Grey Wiese, Angel, оба подарочных окна и заканчивается `wedding-epilogue` без ручной правки состояния.
- Все четыре концовки структурно и по runtime-условиям достижимы; обязательный графовый validator остаётся зелёным.
- `jq empty`, `npm run typecheck`, `npm run build` и `git diff --check` проходят.
- Герои, их утверждённые навыки и стартовые сюжетные экраны не перерабатываются.

## Delta 11 — ручная приёмка наряда Алексисом

Статус: реализована как локальная доработка сцены `alexis-room` и её alias `alexis-room-after-pussy`.

- В панели с короной показываются ровно два исхода приёмки: отличный и достаточный. Проверок d20, автоматического анализа предметов и кнопки провала нет.
- Отличный исход последовательно показывает сертификат «ALEXIS — Алексис Великолепный» и два разрешения на проход в бар. Сертификат добавляется в общий инвентарь как code-native артефакт `alexis-fashion-expert-certificate`.
- Достаточный исход показывает только два разрешения. Разрешения используют утверждённый арт `closed-bar-token.png`, но не добавляются в инвентарь.
- Оба исхода открывают дальнейший проход, пишут взаимоисключающие флаги и сохраняются в локальном состоянии сцены; повторная выдача заблокирована.
- Плохой результат остаётся мастерским устным решением: пока ни одна кнопка не нажата, игроки могут переделывать наряд без изменения состояния.
- Публичная кнопка «Сдать наряд» возвращает из конструктора к Алексису и сохраняет все три выбранных слоя на манекене; до сдачи мастерские кнопки награды не показываются.
- Кнопка «Шаг назад» откатывает по одному этапу `rewarded → submitted → builder → alexis-room → hotel-gallery`; откат `rewarded` создаёт обратные события флагов и удаления сертификата, поэтому награда исчезает и из общего инвентаря.
- Порядок восьми предметов в каруселях верха, низа и акцента задан тремя независимыми последовательностями: части четырёх готовых сетов не совпадают по позиции и не образуют очевидные вертикальные комплекты.

## Delta 12 — полная механика реквизиторской Pussy Sultan

Статус: реализована после утверждения пользователем четырёх состояний комнаты.

- `pussy-prop-room` использует три взаимоисключаемых формальных пути: Линда (Ловкость DC 15), Ламберт (Интеллект DC 15) и Головач Лена (Сила DC 18).
- Провал Линды или Ламберта устанавливает `prop-room-force-only`; оба тонких способа исчезают и не восстанавливаются после перехода между сценами или refresh.
- Провал силы атомарно устанавливает `prop-room-carriers-awakened`, увеличивает `preFinalCombats` и запускает encounter `prop-room-winding-carriers` без дополнительного решения мастера.
- Encounter содержит три отдельных цели с прежним лицом боевого аватара. Победа устанавливает `prop-room-carriers-defeated`; скипетр добавляется в инвентарь только отдельным действием после боя.
- Полное поражение партии не создаёт софтлок: герои встают с 1 HP, носильщики ломаются, `timePressure` увеличивается на 1, а скипетр становится доступен.
- Четыре канонических фона переключаются по флагам: исходная загадка, только силовой путь, пробудившиеся носильщики и состояние после боя.
- Старые флаги и тексты про ледяную полосу, накрытие героев и балдахин больше не создаются активным runtime.

## Delta 13 — два бунгало и пять пропусков от Олвы

Статус: утверждено пользователем для публикации и реализации 4 сентября 2026 года. Эта дельта заменяет активный маршрут Delta 5–8 после закрытого бара; старые разделы остаются только историей итераций.

### Игровой поток

- После освобождения танцоров `closed-bar` ведёт сразу в `guest-bungalows`; артистическая гримёрка исключена из активного маршрута и её старый URL перенаправляется на развилку.
- `guest-bungalows` показывает ровно два направления: открытый путь к Олве и видимый, но закрытый путь к Егорику. Попытка пройти к Егорику до получения доступа открывает пояснение, а не скрывает направление.
- `bungalow-courtyard` использует близкий кадр Олвы рядом с её бунгало. Олва безусловно выдаёт партии доступ, но одновременно предлагает необязательный сайд-квест — решить проблему Полины.
- Принятие и отказ являются двумя явными взаимоисключающими решениями мастера. Оба атомарно устанавливают `olva-bungalow-access-issued` и добавляют `guest-bungalow-pass` в количестве 5 для всей партии.
- Принятие ведёт в существующую линию `couples-session-entry`; любой её выход возвращает на развилку. Отказ ведёт на отдельный экран `olva-passes-handoff`, где видны ровно пять плоских золотых пропусков, затем возвращает на развилку.
- После выдачи пропусков путь к Егорику открывается. `egorik-bungalow-reveal` теперь продолжает основной маршрут в `groom-tunnel`.

### Данные, ассеты и FSD

- Канонические сцены и переходы хранятся в `content/campaigns/penisuela-session-preview.json` и `content/campaigns/penisuela-gallery-gameplay.json`; мастерский гайд синхронизируется с ними.
- Три опубликованных фона: `bungalows-two-paths.png`, `bungalow-courtyard.png` и `olva-quest-declined-passes.png`, все 1536 × 864 и зарегистрированы в `assets/concepts/manifest.json`.
- `GuestBungalowsAdventure` отвечает только за представление трёх новых экранов и видимый guard пути Егорика. Изменения флагов и инвентаря по-прежнему выполняет общий событийный controller через формальные story actions.
- `CampaignPlayPage` выбирает специализированный виджет для трёх scene id. `ClosedBarAdventure` больше не выдаёт пропуск Стасом, а `LateStoryAdventure` возвращает завершённый сайд-квест на развилку.
- Новых полей схемы нет: используются существующие `scenes[]`, `storyScenes[]`, `outcome.flags`, `outcome.inventory.acquire` и `outcome.inventory.quantities`.

### Критерии готовности

- На развилке одновременно видны оба бунгало; до решения Олвы путь Егорика закрыт с понятным сообщением, после любого решения открывается без расходования пропусков.
- У Стаса нет мастерского действия и предмета-пропуска; активный маршрут не показывает и не требует артистическую гримёрку.
- На экране Олвы доступны ровно два решения: принять сайд-квест или отказаться. Оба выдают ровно пять пропусков; отказ показывает отдельный утверждённый кадр передачи.
- Принятый сайд-квест остаётся необязательной петлёй и возвращает к двум бунгало, не перескакивая к тоннелю. После сцены Егорика основной путь ведёт к `groom-tunnel`.
- Undo, reset и refresh воспроизводят выбор, пять пропусков и доступ к Егорику через общий журнал событий.
- `jq empty`, проверка уникальности id и ссылок на ассеты, `npm run typecheck`, `npm run build`, `npm run build:pages` и встроенная браузерная проверка на 1024 и 1280 px проходят без ошибок и горизонтального переполнения.

## Delta 14 — видимый проход из бара к бунгало

Статус: арт выбран пользователем и передан в публикацию и реализацию 4 сентября 2026 года.

- Общий экран `closed-bar` использует `closed-bar-v7.png` и одновременно показывает три интерактивные зоны: Стаса, сцену с пятью танцорами WOK и закрытый проход к гостевым бунгало.
- До флага `dance-troupe-freed` клик по проходу открывает общий `SceneDecisionModal` с пояснением блокировки и действием перехода к танцорам.
- После освобождения труппы тот же hotspot вызывает существующее действие `continue-1-guest-bungalows`; прямой обход событийного контроллера не используется.
- Для новой точки используются существующие `SceneHotspotLayer`, `SceneDecisionModal` и `SceneTextPanel`; новый локальный интерфейс или CSS не создаётся.

## Delta 15 — Бунгало Егорика: утверждённые арты и бой (7 сентября 2026)

Два утверждённых арта опубликованы в manifest и session-preview. Каноническая встреча `egorik-bungalow-guards` содержит трёх призванных рунических стражей и повторно использует утверждённую аватарку. Вход после пропусков Олвы не запускает бой: мастер начинает его внутри. Победа и поражение передают факты и открывают тоннель; только победа даёт союзников. `EgorikBungalowAdventure` использует общий `SceneCombatPanel`, боевой HUD, D20Roller, консоль мастера и журнал с отменой в пределах сцены. Старые ID исходов сохранены. Сюжет с водой и мостиком больше не исполняется.

Баланс для пяти героев: три стража по 24 HP, AC 12 (10 после раскрытия слабости), атака +4, 1d6+2. Числа взяты из существующей охраны бара; тип урона физический соответствует мечам. Допущение реализации: при полном поражении Настасья отсекает призыв, партия восстанавливается до 1 HP, союзный бонус не выдаётся. Послебоевой арт остаётся планом; итог читается в общей модалке поверх боевого фона.

## Delta 16 — Разговор, браслет и путь к Kreed (7 сентября 2026)

Заменяет послебоевую модалку и немедленную выдачу правды из Delta 15. Пользователь утвердил последние версии общего кадра и крупного плана браслета. Оба арта опубликованы в manifest и interactionViews текущей сцены.

После завершения боя открывается общий кадр. Через общую панель мастера с короной «Показать браслет» раскрывает правду курьера и четыре сюжетные улики; «Общая сцена» возвращает пару. Только после раскрытия на общем кадре появляется «Идти к Kreed», переход в существующий groom-tunnel. Каноническое уточнение: Егорик передал конверт Kreed у входа в служебный тоннель; Kreed ушёл под сцену к комнате подготовки.

Два переключателя используют repeatable у automatic-действий, общий контроллер и журнал; обычные действия остаются однократными. Переключения не расходуют ресурсы и не повторяют награду за бой. Выбранный ракурс сохраняется, отмена восстанавливает предыдущее состояние. Старые ID исходов сохранены; старые сохранения с уже известными уликами сохраняют доступ к тоннелю.

## Проход к Kreed — реализация 7 сентября 2026

Авторизация: «давай все собирать в коде» после просмотра всей серии, включая дверь целиком v2. Приняты последние варианты шести кадров; отдельный арт бокового обхода не создаётся: найденный обход заканчивается у показанной двери.

Порядок: зарегистрировать шесть утверждённых PNG; заменить боевую постановку groom-tunnel каноническими действиями и ракурсами; расширить общий контракт проверок источниками, бонусом предмета и изменением DC по флагу; подключить отдельный widget на прежнем URL; проверить оба пути, общий заряд Лены, преимущество Торина, отмену и восстановление. Общие SceneTextPanel, SceneCheckPanel, SceneDecisionModal, SceneMasterControl и GameMasterConsole переиспользуются. Никаких новых зависимостей.

Схема: storyActions.requirements (герой, предметы, способности, расход общего combatActionId); check.modifierBonus и dcModifiers. Весь проход — одна локация, смена картинки не восстанавливает ресурсы. Торин: Сила DC 12 с преимуществом; успешный скан Лены снижает DC до 8. Кулон: Интеллект +4, DC 12, один общий заряд Видеонаблюдения/location. Неудача Торина открывает проход шумно; провал скана не мешает продолжить. Мастер может принять альтернативную заявку без блокировки состава партии.

Стабильные scene id и старые журналы сохраняются; новая цепочка не списывает Doom/HP и не запускает бой. Комната Kreed сохраняет последующую сцену освобождения и не раскрывает невесту на входе. Публикация сайта и git-операции не входят в работу.


### Встреча с Kreed — подключение 2026-09-07

Утверждённый groom-hideout.png подключён к встрече и признанию. Сохранены scene/action ids и флаг kreed-rescued для старых журналов. Единственное действие встречи — «Поговорить с Kreed», без проверки, Doom и расхода. Обе кнопки разговора работают через общий SceneMasterControl; описание — SceneTextPanel appearance="narration". Контрольные размеры: 1280×720 и 1024×768.


## Вилла и проверка доверия Grey Wiese — 2026-09-07

Подключены утверждённые вилла, закрытая дверь и Grey Wiese v3. Grey Wiese — эльф в закрытом чёрном платье. Один герой проходит Харизму DC 12: успех даёт доверие, провал — личное сопровождение. Дверь открывается при обоих исходах без Doom и расхода использований. Общий SceneCheckPanel, SceneTextPanel с «Рассказчик», управление из короны. Вход внутрь отдельным действием; состояние и отмена используют журнал сессии. Сохранены стабильные ключи флагов и успешного действия старых сохранений.


### Уточнение доверия: просьба Kreed

Первый провал Харизмы DC 12 больше не открывает дверь. Появляется «Попросить Kreed»: отдельная проверка NPC Kreed DC 8, модификатор 0. Узнав Kreed, Grey Wiese при успехе доверяет группе, при провале впускает под сопровождением. Общий SceneCheckPanel; NPC не входит в партию, журнал сохраняет его actor ID. Старые уже открытые сцены сохраняют состояние. Публичное написание имени везде Grey Wiese, технические `graywise-*` не меняются.

## Спальня — утверждённая реализация 7 сентября 2026

Авторизация: «давай» после утверждения двери v1 и спальни v8 и предложения подключить раскрытие кнопкой короны. Публикуются только эти два PNG, без изменения изображения. Схема не расширяется: существующие interactionViews, automatic actions и flags.

На bedroom-reveal начальный кадр — закрытая дверь и неспойлерный Рассказчик. «Открыть дверь спальни» записывает локальный bedroom-door-revealed без броска и переключает арт/текст. «Продолжить к распаковке» сохраняет прежний ID перехода и graph-owned igor-revealed. Старые сохранения с igor-revealed сразу показывают открытый кадр. Общие CampaignScene, SceneTextPanel, SceneMasterControl и GameMasterConsole; отдельный widget BedroomRevealAdventure без новых стилей и движка. Проверки: два состояния, скрытый переход до раскрытия, сохранение, scoped undo, переход к распаковке, typecheck/build/build:pages и встроенный браузер.


## Единый клик по дверям — 7 сентября 2026

Авторизация: «если это дверь на экране, то клик по двери должен ее открывать … поправь это везде». Заменяет прежний вход в спальню только из короны. Аудит действующих widgets выявил пропуски в BedroomRevealAdventure, GraywiseDoorAdventure и GroomTunnelAdventure; гостиница, бар, развилки и пространственные переходы LateStoryAdventure уже используют клики.

Добавлены зоны общего SceneHotspotLayer: спальня; дверь Grey Wiese (проверка группы, помощь Kreed после провала, вход после открытия); дальняя дверь коридора, действие Линды на закрытой двери, проход за ней, финальная дверь к Kreed. Все запускают прежние обработчики с теми же guards и ресурсами. «Попросить Kreed» также остаётся отдельной мастерской кнопкой. Сюжетное продолжение распаковки остаётся в короне.

Общий слой получил необязательную привязку background с cover/contain; новые координаты задаются в процентах изображения и пересчитываются при загрузке арта и изменении размеров. Старые зоны без background сохраняют прежнее позиционирование. Новых полей канона, механик и ассетов нет. Правило закреплено в AGENTS.md и implement-dnd-campaign-ui.


## Дельта 7 сентября: автоматическая последовательность боёв лорда Нетака

Автор уточнил: победа над лордом Нетаком → ролик поражения и превращения → второй арт и новый бой с драконом. Видео пока отложено; без source переход немедленный. Ручные переключатели удалены до внедрения. Публикуются два утверждённых PNG без обработки. `bossSequence` в canonical gameplay связывает две новые встречи, второе interactionView и необязательный ролик. Старые id маршрутов и старые журналы не сбрасываются. Флаг ручного прототипа игнорируется.

FSD: чистый builder событий `features/navigate-campaign-scene/model/bossSequenceCommands.ts`; атомарная команда `useGallerySession`; модель контракта `entities/campaign-session`; экран `widgets/AndreyBossAdventure`; маршрутизация в page. Переиспользованы SceneCombatPanel/CombatEncounterHud/D20Roller, SceneTextPanel, SceneMasterControl, GameMasterConsole и CombatSkillVideoOverlay. Последний получил необязательную скорость: 1× для превращения, прежние 2× для навыков.

Переход после добивания объединяется с командой удара для undo. Загрузка журнала и повторный effect не дублируют вторую встречу. Урон героям и постоянные расходы сохраняются; восстановление battle uses штатное. Конечные выходы требуют победы над драконом. Поражение не считается победой, аварийный путь сохранён.

Критерии: реальные боевые атаки обеих фаз, новая инициатива/полные HP дракона, reload/undo, ожидание video-ended и идемпотентность, отсутствие смены фаз в короне, арты на 1024/1280 px, тесты и сборки. База 80/45 HP при AC 15 и +6 к атаке — рабочая, баланс требует отдельного игрового прогона. Колбы, видео, сумка и сюжетный мост от виллы отложены.


## Подключённая цепочка виллы — 2026-09-07

Основной runtime: `bedroom-reveal` → `igor-unboxing` (необязательный сертификат Алексиса → косметическая Birkin Торина) → `andrey-villa-breach` (взрыв без персонажа, появление лорда Нетака, затем признание) → `last-take-boss` (автоматически первый бой, затем дракон). Утверждённые источники: Angel v2 и лорд Нетак v4, опубликованы без изменения изображения.

Новый канон: лорд Нетак устроил хаос мальчишника ради срыва свадьбы, придумал механизм и обманом привлёк Лену, вручив фальшивый Oscar; её проектор нужен для запуска катастрофы. Он переносит героев и трёх заложников в своё пространство. Личная причина ненависти к свадьбе пока не определена.

Прежние узлы `wedding-reminder`…`final-choice` — совместимость старого маршрута. Их требования не блокируют новую цепочку. Победа открывает существующие последствия shutdown без автоматического согласия на свадьбу/публикацию. Полная переработка эпилога, колбы как цели, временные союзники и видео превращения остаются отдельными задачами. Старые графовые контракты и расчёт времени для заменённого участка устарели; актуальные исполняемые переходы находятся в `content/campaigns/penisuela-gallery-gameplay.json`, художественные тексты — в preview JSON и guide.

### Утверждённый скин верхней сумки — 2026-09-07

Вместо фокуса на сцене используется прозрачная `ui/inventory-birkin-skin.png`, выбранная автором. Общий контроллер передаёт `inventoryArtwork` во все адаптеры игрового экрана. Обмен включает облик верхней кнопки без выдачи нового предмета; undo возвращает старую иконку. Проверка: `qa/birkin-button-skin-2026-09-07.md`.

## Утверждённый счастливый финал — 2026-09-08

Автор утвердил последовательность и три выбранных арта: villa-after-andrey-v1, couple-voice-reset-v3, happy-altar-v5. Разрешено собрать их в игре. Публикуем PNG без изменения содержания. Основной маршрут после победы над драконом: видео гибели лорда Нетака → villa-after-andrey → couple-voice-reset → wedding-epilogue. Пока видео нет, переход к вилле выполняется сразу.

Схема: необязательный bossSequence.aftermath с returnSceneId и deathVideo {title, source?}; завершение ролика хранится флагом andrey-death-video-finished. Повтор завершения безопасен, поражение не запускает финал. Автоматический переход и конец ролика принадлежат добивающему действию для отмены. HP и инвентарь сохраняются. Голосовой сброс — сюжетное действие без броска и расхода: сначала реплика пары, затем переход к свадьбе. Торин на арте обычного взрослого роста; Angel в свадебном платье, Kreed в костюме, все гости празднично одеты.

Переиспользуем VillaFinaleAdventure, SceneTextPanel с narration, SceneMasterControl через CampaignScene, GameMasterConsole и CombatSkillVideoOverlay. Вилла показана снаружи; клик по входной двери через общий SceneHotspotLayer вызывает тот же обработчик перехода к паре, что и действие мастера. Координаты привязаны к PNG и contain. Остальные сцены меняются мастерскими сюжетными действиями. Старые URL и исходы остаются совместимыми, но не блокируют новый основной маршрут. Проверки: схема и ссылки, тест последовательности/видео/отмены/перезагрузки, typecheck, build, build:pages, браузер 1024 и 1280 px.


## Пустая спальня после взрыва — 8 сентября 2026

В `andrey-villa-breach` начальный арт — `villa-breach-empty.png`, без персонажей. Кнопка короны «Появление лорда Нетака» (`show-andrey-appearance`) устанавливает `andrey-appeared` и открывает `andrey-appearance` с прежним артом босса. Далее идут существующие признание и перенос. Появление сохраняется и отменяется; признание требует появления. Сохранённое ранее признание имеет приоритет над новым промежуточным состоянием. Маршруты, бои, HP и награды не меняются.

## Подключение плохого финала — 8 сентября 2026

Утверждено автором: «окаю го делать». После падения пяти героев в любой фазе лорда Нетака: разрушенная вилла (`bad-ending-villa`) → трансляция свадьбы лорда (`bad-ending-netak-wedding`) → темница Angel, Kreed и Grey Wiese (`bad-ending-magical-prison`). Свадьба снимается с посторонним взрослым двойником Kreed. Пятеро героев живы и остаются на захваченном острове. Голоса Angel и Kreed активируют окончательное подчинение механизма, запущенного прежде через проектор Лены. Эта ветка заменяет прежнее аварийное спасение только при поражении Нетаку; победный финал сохраняется. Канон и механика опубликованы в session-preview.json, gallery-gameplay.json и синхронном гайде; арты wedding-v1 и prison-v4 опубликованы без изменения изображения. Проверка: qa/netak-bad-ending-2026-09-08.md.
