# Модальные окна боя со стражей — 16 сентября 2026

## Поведение

После провала запугивания появляется обязательное окно «Разговор окончен»:

> Угрозы не производят на Pussy Sultan ни малейшего впечатления. Его усмешка медленно гаснет, уступая холодному раздражению. По короткому зову хозяина в номер входят трое стражников. Лязг оружия ставит точку в разговоре.

Единственная кнопка — «Принять бой». Провал записывает призыв стражи, но встреча и увеличение счётчика боёв теперь происходят только после нажатия кнопки. Окно определяется сохранённым флагом, поэтому восстанавливается после перезагрузки. Отмена начала боя возвращает подтверждение; отмена проверки убирает его. Уже начатые сохранённые бои продолжаются без дополнительного окна.

В окне спуска в бар без полного комплекта пропусков удалена кнопка возвращения. Осталась «Принять бой». Обе модалки используют общий SceneDecisionModal с dismissible=false: нет крестика, закрытия по Escape или по фону. Успешное запугивание сохраняет прежние три жетона и испуганный арт. Состав стражи, DC и правила боя не менялись.

## Изменённые файлы

- `src/widgets/campaign-scene/ui/HotelGalleryAdventure/HotelGalleryAdventure.tsx`: обязательные модалки, фон призванной стражи до боя, блокировка повторных мастерских решений во время подтверждения.
- `src/features/navigate-campaign-scene/model/useGallerySession.ts`: начало боя отделено от провала проверки.
- `content/campaigns/penisuela-gallery-gameplay.json`, `docs/campaigns/penisuela/gameplay.json`: литературный текст и подпись существующего действия принятия боя.
- `content/campaigns/penisuela-session-preview-guide.md`, `docs/campaigns/penisuela/script.md`, `implementation-plan.md`, `workflow.json`: синхронизация поведения.
- `scripts/test-pussy-battle-confirmation.mjs`: проверка реального контроллера и сохранённого журнала с тестовыми React hooks и памятью вместо браузерного localStorage.

## Проверки

- `node scripts/test-pussy-battle-confirmation.mjs`: PASS — провал не запускает бой, восстановление сохраняет ожидание, подтверждение вызывает трёх стражников, счётчик увеличивается один раз, откат боя/проверки и успешное запугивание работают.
- `npm run test:pussy-audience`: 8/8.
- `node scripts/test-hotel-bar-access.mjs`: PASS — отсутствие, частичные и полные пропуска, восстановление, отмена и неверные предметы.
- `npm run build`, `npm run build:pages`: PASS, включая typecheck. Существующее предупреждение о крупных chunks сохраняется.
- `jq empty`, равенство канонического и рабочего gameplay, уникальность action IDs, совпадение failureText/failureResolution/threatFailure, `git diff --check`: PASS.

Новые канонические изменения ограничены подтверждением начала боя и отсутствием ухода из окна бара. Баланс для пяти героев не менялся. По сохранённому указанию пользователя браузер не открывался; визуальная проверка и реальные клавиатурные события не выполнялись. Commit, push и deployment не выполнялись.
