# Утверждённая Birkin для верхней кнопки — 2026-09-07

Утверждённый пользователем скин опубликован как `assets/concepts/campaigns/penisuela/ui/inventory-birkin-skin.png`, PNG 1254×1254 с alpha. Файл скопирован без растровой обработки; совпадение с выбранным вариантом проверено побайтово. Запись manifest: `penisuela-ui-inventory-birkin-skin`.

## Изменения

- `content/campaigns/penisuela-gallery-gameplay.json`: существующий `itemSkins` использует новый прозрачный арт целиком вместо фрагмента сцены с руками. Условия, эффекты обмена и характеристики предмета не менялись.
- `src/features/navigate-campaign-scene/model/useGallerySession.ts`: общий контроллер отдаёт `inventoryArtwork`, вычисленный из существующего флага подарка.
- Все 16 адаптеров `CampaignScene` в `src/widgets/campaign-scene/ui/` передают этот облик общей верхней кнопке. Поэтому возврат в более раннюю сцену не возвращает старую иконку.
- `src/shared/ui/ArtworkFocus/ArtworkFocus.module.css`: убрано скругление прямоугольника, чтобы отдельная прозрачная иконка сохраняла свой полный силуэт. Круглая рамка боевого меню по-прежнему управляется её общим компонентом.
- `assets/concepts/manifest.json`, `content/README.md`, мастерский guide, `docs/campaigns/penisuela/art-plan.json` и `workflow.json`: зарегистрированы утверждение, путь, назначение и проверка.

Новый визуальный канон: после обмена сертификата верхняя кнопка открытия инвентаря выглядит как одобренная изумрудная Birkin с золотой фурнитурой. Нового предмета или ячейки нет. Сумка семи профессий сохраняет ID, владельца, количество, способности и заряды; скин отменяется вместе с обменом.

## Проверки

- `npm run typecheck`, `npm run build`, `npm run build:pages`: PASS. Прежнее предупреждение Vite о крупных chunks остаётся.
- `node scripts/test-villa-finale.mjs`: PASS — расход сертификата, косметический характер обмена, отсутствие дубля, сохранение и отмена, свойства сумки и guards цепочки.
- `jq empty` для изменённых JSON, `git diff --check`: PASS.
- Уникальная запись manifest, существование canonical пути, совпадение PNG с утверждённым вариантом и отсутствие `inventory.acquire` у обмена: PASS.
- Встроенный браузер: `igor-unboxing` на 1280×800 — обмен включает новый прозрачный силуэт верхней кнопки. Открытый инвентарь содержит прежний пропуск, новой сумки в списке нет.
- `bedroom-reveal` на 1024×768: возврат к предыдущей сцене сохраняет Birkin; перезагрузка сохраняет тот же путь изображения. Скриншоты просмотрены, горизонтального overflow нет. Ошибок/предупреждений в консоли нет.
- Тестовый сертификат и обмен отменены через интерфейс. Сессия не сбрасывалась; после отмены возвращается исходная иконка.

Проверка относится к скину и сохранению его состояния. Остальные механики, баланс и несвязанные страницы повторно не проходились. Commit, push и deployment не выполнялись.
