# Проход к Егору Криду — проверка 7 сентября 2026

Авторизация: «давай все собирать в коде». Шесть последних просмотренных кадров приняты для реализации, внесены в manifest и art-plan. Отдельный неподготовленный арт бокового обхода не используется: его результат показан у общей двери. Новые канонические факты: коридор без врагов, автоматическое решение Бубсильды, обход Лены, дверь Линды, тележка Торина, опциональное сканирование Лены и шумное вступление к Криду. Последующее освобождение Крида сохранено.

## Проверки

- Typecheck, обычная сборка, Pages-сборка: PASS. Предупреждение Vite о крупных чанках остаётся.
- `npm run validate:penisuela-playable`: PASS, 744 проверки, все четыре финала достижимы.
- `node scripts/test-groom-tunnel.mjs`: PASS. Оба пути, доступность по герою/HP/владельцу предмета, общий ресурс, DC 12/8, бонус кулона +4, критические 1/20, преимущество, провалы, отсутствие новых HP/Doom-эффектов, сохранение и отмена.
- `node scripts/test-egorik-bungalow.mjs`: PASS, регрессия предшествующей сцены.
- Встроенный браузер, 1280×720: коридор → автоматический Баланс Бубис → дверь целиком → reload → полёт Линды → тамбур.
- Скан тележки: физический 11 + 1 = DC 12, успех. У Торина DC 8. Цифровые 2d20 дали 5 и 19, общий D20Roller выбрал 19 и перенёс оба значения в SceneCheckPanel; проход освободился.
- Встроенный браузер, 1024×768: окно проверки и герой с аватаркой помещаются; поочерёдная отмена возвращает кадры и заряд Лены.
- Альтернативная ветка: физическая 1 у Лены показывает общий critical effect и открывает шумный обход. После Линды скан тележки disabled; у Торина DC 12. Физические 1/2 открывают проход шумно. «Войти к Егору Криду» открывает groom-preparation-room с реакцией на шум.
- Консоль браузера: пустой список error/warn. Битых изображений нет. На 1024×768 scrollWidth = clientWidth = 1024, scrollHeight = clientHeight = 768.
- Тестовые сюжетные действия отменены штатной командой; участок возвращён к началу.

## Границы

Сцена использует существующую локацию и общий ledger; ракурсы не восстанавливают заряд. Альтернативные заявки принимает мастер отдельными кнопками. Дополнительные бонусы следующей атаки от Баланса Бубис и возвращения Линды к обычному росту остаются в ручном ведении мастера согласно текущим карточкам; новая боевая автоматизация для них не добавлялась. Git commit, push и deployment не выполнялись.

Основные изменения: GroomTunnelAdventure, model/groomTunnel, storyActionRules, общий useGallerySession, SceneCheckPanel/D20Roller, маршрутизация CampaignPlayPage; два канонических JSON, гайд, manifest, шесть PNG; синхронизация scoped gameplay/art-plan/story-graph/implementation-plan.

Финальная сверка: JSON и ссылки на сцены, героев, предметы, способности, расходуемое действие и арты — PASS; manifest без повторяющихся campaign id. На 1280×720 scrollWidth = clientWidth = 1280, scrollHeight = clientHeight = 720; текстовый блок целиком внутри окна (x 106–1174), внутреннего переполнения нет. Временный viewport сброшен.
