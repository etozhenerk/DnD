# Рассказчик без диалогов — 11 сентября 2026

По уточнению автора все вводные текущих игровых сцен и их ракурсов содержат только литературное описание. Прямая речь, обмен репликами и пересказ разговора не подставляются в блок рассказчика. Предисловие со слайдами не изменено.

Переписаны вводные Алексиса, Олвы, Стаса, Егорика, Kreed, виллы и концовок. Для трёх состояний разговора со Стасом и существующих ракурсов Егорика используются отдельные `interactionViews[].readAloud`. Описание тоннеля больше не заменяется результатом действия. Аудиенция Pussy Sultan не подставляет `dialogues` в описание. После освобождения танцоров к вводной не приклеиваются их реплики о награде. В квесте Олвы `endings[].readAloud` и `reward.presentation.readAloud` отделены от подробных результатов и речи Олвы; формальные эффекты и реплики остались прежними.

Сценарий мастера синхронизирован; вынесенные из вводных сведения сохранены в отдельном разделе для разговоров. Правило записано в `AGENTS.md` и `content/README.md`.

Проверки:

- `node scripts/test-penisuela-narration.mjs`: 98 текстов сцен/состояний без прямой речи, отдельные вводные к исходам/награде, сохранённые реплики, синхронный гайд.
- `node scripts/test-olva-quest.mjs`: доказательства, 243 распределения голосов, три исхода, награды и отмена.
- `node scripts/test-olva-recruitment.mjs`: приглашение Стаса, обе ветки, отмена, загрузка, сброс, необязательные ключи и совместимость.
- `node scripts/test-penisuela-story-sync.mjs`, `node scripts/test-penisuela-navigation-regressions.mjs`, `node scripts/validate-penisuela-current-release.mjs`: успешно.
- `npm run typecheck`, `npm run build`, `npm run build:pages`: успешно; прежнее предупреждение Vite о крупных чанках.
- `jq empty`: JSON корректны.
- Сравнение с копиями до правок: предисловие побайтово неизменно; формальные данные gameplay и квеста Олвы совпадают после исключения только художественных полей.
- Граф неизменён: `Graph valid: 29 nodes, 1 starts, 2 endings, routes 142-158m`; прежнее предупреждение о кратчайшем маршруте ниже рекомендации 150 минут.

Во встроенном браузере проверены описание Олвы при входе, Стас после приглашения и аудиенция Pussy Sultan. Контрольные размеры 1280×800 и 1024×768. В рассказчике только проза; ошибок браузера и горизонтального переполнения нет. Изменения этой итерации не затрагивают боевую механику, поэтому полное боевое прохождение не повторялось.
