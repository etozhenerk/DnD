# Музыка бункера и виллы — 16 сентября 2026

Все шесть существующих sceneBlocks теперь имеют единое назначение музыки. Бункер после служебного коридора использует «Базар золотых монет»: укрытие Kreed, раскрытие и выход к вилле продолжают один трек. Вилла целиком вне боя использует «Затишье перед штормом»: дверь Grey Wiese, спальня, распаковка Angel, появление Нетака, арена, последствия и финальные экраны не меняют этот фон между собой.

«Каменный шёпот» остаётся строго в groom-tunnel. Для этого существующее soundtrack.scenes хранит только одно явное исключение; оно приоритетнее назначения блока. Копии назначений всех остальных экранов удалены. Боевой плейлист по-прежнему приоритетен в бою; обе фазы Нетака используют один плейлист без перемотки между фазами. Титры и видео сохраняют свои самостоятельные правила.

Изменены `content/README.md`, канонический `content/campaigns/penisuela-gallery-gameplay.json`, соответствующий `content/campaigns/penisuela-session-preview-guide.md`, рабочий `docs/campaigns/penisuela/gameplay.json`, модель `src/entities/campaign-session/model/soundtrack.ts`, проверки `scripts/test-campaign-soundtrack.ts` и `scripts/test-campaign-soundtrack-player.mjs`, implementation-plan и workflow. Новых сюжетных фактов, правил, ассетов и миграций сохранений нет.

Проверки:

- `node scripts/run-typescript-test.mjs scripts/test-campaign-soundtrack.ts`: PASS — охвачены все шесть блоков, единственное исключение коридора, известные сцены/встречи/треки/файлы/manifest, условия тишины бара и отсутствие коридорного трека вне тоннеля.
- `node scripts/test-campaign-soundtrack-player.mjs`: PASS — настоящий hook и плеер с тестовыми hooks/media. Переходы с unmount и задержкой сохраняют source/currentTime и не вызывают дополнительные play/pause внутри бункера и всей виллы. Переход в коридор меняет фон; вход к Kreed выключает коридорный трек. Обе фазы Нетака продолжают общий боевой плейлист; завершение боя возвращает фон виллы. Проверки предыдущих четырёх блоков, повторов и затухания также проходят.

- `npm run build`, `npm run build:pages`, включая typecheck: PASS; прежнее предупреждение Vite о крупных chunks. `jq empty`, `git diff --check` и сверка soundtrack канона с рабочим gameplay: PASS.

Браузер не открывался по указанию пользователя; реальное звучание и autoplay не проверены. Commit, push и deployment не выполнялись.
